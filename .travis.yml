language: csharp
solution: BookStoreContribe.sln
mono:
  - latest
script:
  - msbuild /p:Configuration=Debug BookStoreContribe.sln
before_install:
  - sudo apt-get install nunit-console
before_script:
  - nuget restore BookStoreContribe.sln
after_script:
  - nunit-console ./BookStoreAPI.Tests/bin/Debug/BookStoreAPI.Tests.dll